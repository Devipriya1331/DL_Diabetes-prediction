# DL_Diabetes prediction
A Keras-based neural network model trained on the Pima Indians Diabetes Dataset to predict the likelihood of diabetes using medical diagnostic features. The project demonstrates model training, evaluation, saving, and loading for prediction.

# 🩺 Diabetes Prediction using Deep Learning

A deep learning-based diabetes prediction system built using **Keras and TensorFlow**. The model is trained on the **Pima Indians Diabetes Dataset** to predict whether a patient is diabetic based on medical attributes.

## 🔍 Features
- Binary classification using Artificial Neural Networks (ANN)
- Model training, evaluation, saving, and loading
- Prediction on patient data

## 🛠️ Technologies Used
- Python
- NumPy
- TensorFlow / Keras

## 📊 Dataset
- Pima Indians Diabetes Dataset  
- 8 input features and 1 output label

## 🧠 Model Architecture
- Input Layer: 8 neurons  
- Hidden Layers: Dense layers with ReLU activation  
- Output Layer: 1 neuron with Sigmoid activation

## 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install numpy tensorflow keras

## Train the model:

    ```bash
    python train.py


## Test the model:

 ```bash
python test.py
